.factory-routines
	.row.wrapper.border-bottom.white-bg.page-heading
		.col-lg-12
			.pull-left
				h2 Routines
				p Define routines for business logic, computed properties, and document actions.
			.controls.pull-right
				button.btn.btn-xs.btn-primary(ng-click="newRoutine()")
					i.fa.fa-plus
					span Create Routine
	br/

	.row.wrapper
		.col-lg-12
			.ibox
				.ibox-content
					table.table.table-hover.table-striped.table-condensed
						thead
							tr
								th Name
								th Description
								th Type
								th(width="75")
						tbody
							tr(ng-repeat="routine in routines | orderBy: 'type'")
								td(ng-bind="routine.name")
								td(ng-bind="routine.description")
								td(ng-bind="routine.type  | dictionary:routineTypes")
								td(align="right")
									button.btn.btn-xs.btn-default(ng-click="deleteRoutine(routine)")
										i.fa.fa-trash
									button.btn.btn-xs.btn-primary(ng-click="editRoutine(routine)")
										i.fa.fa-pencil
							tr(ng-if="!routines.length")
								td(colspan="99")
									.text-muted No Routines Yet
